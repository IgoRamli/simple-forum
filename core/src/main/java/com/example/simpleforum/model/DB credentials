user=simple_forum_app
pass=simple_forum_app

How to access psql with a user other than "postgres":
1. Create a user with `sudo adduser <username>`
   > This creates a user and role within Ubuntu
2. Go to user shell `sudo su - <username>`
   > Directs you to a shell where you can login to psql as <username>
3. Create DB as the user with `createdb <dbname>`
4. `psql -d <dbname>`